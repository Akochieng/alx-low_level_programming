#include <stdio.h>

/**
  *mul - multiplies two intergers
  *@a: one of the intergers to be multiplied
  *@b: the other interger to be multiplied
  *
  *Description: multiplies intergers a and b
  *and returns the result
  *Return: a * b
  */

int mul(int a, int b)
{
	return (a * b);
}
