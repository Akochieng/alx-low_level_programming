#include <stdio.h>
#include "main.h"
/**
  *int _abs - computes the absoulute value of an interger
  *
  *Return: the absolute value
  */
int _abs(int a)
{
	int result;

	return (result = a > 0 ? a : -a);
}
