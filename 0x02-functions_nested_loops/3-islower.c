#include "main.h"
#include <stdio.h>

/**
  *_islower - checks whether character is lowercase
  *
  *Return: 1 is lowercase, 0 otherwise
  *
  */
int _islower(int c)
{
	int result;
	
	return result = c >= 'a' && c <= 'z' ? '1' : '0';
}
